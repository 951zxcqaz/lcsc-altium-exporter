<UserControl x:Class="Transform.App.Views.Components.ComponentListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="260" d:DesignWidth="560">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 元件列表（核心显示区） -->
        <ListView Grid.Row="0"
                  ItemsSource="{Binding Components}"
                  SelectedItem="{Binding SelectedComponent}"
                  Background="White" BorderBrush="#E0E0E0" BorderThickness="1"
                  FontSize="13"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="编号" DisplayMemberBinding="{Binding LcscId}" Width="100" />
                    <GridViewColumn Header="名称" DisplayMemberBinding="{Binding Name}" Width="150" />
                    <GridViewColumn Header="型号" DisplayMemberBinding="{Binding ManufacturerPart}" Width="120" />
                    <GridViewColumn Header="品牌" DisplayMemberBinding="{Binding Manufacturer}" Width="100" />
                    <GridViewColumn Header="封装" DisplayMemberBinding="{Binding Package}" Width="100" />
                    <GridViewColumn Header="渠道" DisplayMemberBinding="{Binding Supplier}" Width="70" />
                    <GridViewColumn Header="规格" DisplayMemberBinding="{Binding SpecSummary}" Width="200" />
                </GridView>
            </ListView.View>

            <!-- 空列表提示 -->
            <ListView.Style>
                <Style TargetType="ListView">
                    <Style.Resources>
                        <Style TargetType="TextBlock" x:Key="EmptyHint"
                               BasedOn="{StaticResource {x:Type TextBlock}}">
                            <Setter Property="Foreground" Value="#999" />
                            <Setter Property="HorizontalAlignment" Value="Center" />
                            <Setter Property="VerticalAlignment" Value="Center" />
                            <Setter Property="FontSize" Value="13" />
                            <Setter Property="Text" Value="还没有添加元件 → 在上方搜索结果中选择一个元件，点击【添加到导出】或双击" />
                        </Style>
                    </Style.Resources>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Components.Count}" Value="0">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListView">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <TextBlock Style="{StaticResource EmptyHint}" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListView.Style>
        </ListView>

        <!-- 操作栏：移除选中/清空 -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,0">
            <Button Content="🗑 移除选中"
                    Command="{Binding RemoveSelectedCommand}"
                    Padding="12,3" Background="#FFF2F2" Foreground="#CC3333"
                    BorderBrush="#CCCCCC" BorderThickness="1" FontSize="12"
                    Cursor="Hand" />
            <Button Content="清空列表"
                    Command="{Binding ClearAllCommand}"
                    Padding="12,3" Background="#FFF2F2" Foreground="#CC3333"
                    BorderBrush="#CCCCCC" BorderThickness="1" FontSize="12"
                    Cursor="Hand" Margin="8,0,0,0" />
            <TextBlock Text="{Binding Components.Count, StringFormat='合计：{0} 个待导出元件'}"
                       FontSize="12" Foreground="#666" VerticalAlignment="Center"
                       Margin="12,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
