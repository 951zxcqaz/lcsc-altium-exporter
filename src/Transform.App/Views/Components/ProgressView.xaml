<UserControl x:Class="Transform.App.Views.Components.ProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Transform.App.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="150" d:DesignWidth="600">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="导出进度" FontSize="16" FontWeight="Bold" Margin="0,0,0,8" />

        <ProgressBar Grid.Row="1"
                     Value="{Binding Progress}"
                     Height="24" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <TextBlock Text="已完成: " />
            <TextBlock Text="{Binding CompletedCount}" FontWeight="Bold" />
            <TextBlock Text="  失败: " Margin="16,0,0,0" />
            <TextBlock Text="{Binding FailedCount}" FontWeight="Bold" />
            <TextBlock Text="  总计: " Margin="16,0,0,0" />
            <TextBlock Text="{Binding TotalCount}" FontWeight="Bold" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,0">
            <TextBlock Text="正在处理: " />
            <TextBlock Text="{Binding CurrentComponent}" FontWeight="Bold" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="取消"
                    Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
